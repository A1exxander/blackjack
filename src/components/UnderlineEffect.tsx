export default function UnderlineEffect(): React.ReactElement {
    return (
        <div className={`absolute left-1/2 w-full h-1 -translate-x-1/2 bg-gradient-to-r from-transparent via-gold to-transparent`}></div>
    );
}